PORT=3000

# JWT for auth (use a long random string in production)
JWT_SECRET=your-secret-change-in-production

# Add when you add a DB (e.g. Supabase, MongoDB Atlas, Render Postgres):
# DATABASE_URL=
